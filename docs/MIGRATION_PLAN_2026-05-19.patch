# Step: Update package.json — add @caistech/portfolio-gate ^0.2.0 (devDependencies) + add / upgrade @caistech/corporate-components to ^0.2.0 (dependencies)  (install-portfolio-gate)
# Rule: R13
# Single package.json rewrite. add @caistech/portfolio-gate ^0.2.0 (devDependencies). add / upgrade @caistech/corporate-components to ^0.2.0 (dependencies).

Portfolio-gate (R13) brings the CI smoke tests, errorResponse helper, and static audits. Corporate-components (R1) ships <AuthForm/> in 0.2.0 — required for the R1 swap migration.
--- /dev/null
+++ b/package.json
@@ -0,0 +1,49 @@
+{
+  "name": "deal-findrs",
+  "version": "0.1.0",
+  "private": true,
+  "scripts": {
+    "dev": "next dev",
+    "build": "next build",
+    "start": "next start",
+    "lint": "next lint",
+    "setup:stripe": "node tools/setup-stripe.js",
+    "setup:elevenlabs": "node tools/setup-elevenlabs.js",
+    "setup:elevenlabs:tools": "node tools/setup-elevenlabs-tools.js",
+    "test": "vitest run",
+    "test:watch": "vitest"
+  },
+  "dependencies": {
+    "@11labs/client": "^0.2.0",
+    "@caistech/corporate-components": "^0.2.0",
+    "@caistech/platform-trust-middleware": "^0.4.0",
+    "@caistech/property-services-sdk": "^0.3.0",
+    "@supabase/ssr": "^0.1.0",
+    "@supabase/supabase-js": "^2.39.0",
+    "class-variance-authority": "^0.7.0",
+    "clsx": "^2.1.0",
+    "lucide-react": "^0.303.0",
+    "next": "14.2.35",
+    "openai": "^4.104.0",
+    "react": "^18.2.0",
+    "react-dom": "^18.2.0",
+    "stripe": "^14.10.0",
+    "tailwind-merge": "^2.2.0"
+  },
+  "devDependencies": {
+    "@caistech/portfolio-gate": "^0.2.0",
+    "@types/node": "^20.10.0",
+    "@types/react": "^18.2.0",
+    "@types/react-dom": "^18.2.0",
+    "autoprefixer": "^10.4.16",
+    "postcss": "^8.5.10",
+    "tailwindcss": "^3.4.0",
+    "typescript": "^5.3.0",
+    "vite-tsconfig-paths": "^4.3.2",
+    "vitest": "^1.6.1"
+  },
+  "overrides": {
+    "cookie": "^0.7.2"
+  }
+}
+


# Step: Scaffold routes.config.json  (scaffold-routes-config)
# Rule: R13
# Default top-level route list (homepage, /pricing, /about, /contact, /login, /signup, /forgot-password, /privacy, /terms, /api/health). Edit to match your product's actual routes before running the smoke test.
--- /dev/null
+++ b/routes.config.json
@@ -0,0 +1,37 @@
+{
+  "baseUrl": "https://deal-findrs.vercel.app",
+  "routes": [
+    {
+      "path": "/"
+    },
+    {
+      "path": "/pricing"
+    },
+    {
+      "path": "/about"
+    },
+    {
+      "path": "/contact"
+    },
+    {
+      "path": "/login"
+    },
+    {
+      "path": "/signup"
+    },
+    {
+      "path": "/forgot-password"
+    },
+    {
+      "path": "/privacy"
+    },
+    {
+      "path": "/terms"
+    },
+    {
+      "path": "/api/health",
+      "expectedStatus": 200
+    }
+  ]
+}
+


# Step: Scaffold auth.config.json  (scaffold-auth-config)
# Rule: R1
# Per-product auth path map used by portfolio-gate-smoke-auth. The four legs are wired against the conventional paths (/login, /signup, /forgot-password, /api/auth/*) — edit if your product diverges.
--- /dev/null
+++ b/auth.config.json
@@ -0,0 +1,12 @@
+{
+  "baseUrl": "__OVERRIDE_FROM_CI__",
+  "loginPath": "/login",
+  "signupPath": "/signup",
+  "forgotPasswordPath": "/forgot-password",
+  "magicLinkPath": "/login",
+  "loginActionPath": "/api/auth/login",
+  "signupActionPath": "/api/auth/signup",
+  "forgotPasswordActionPath": "/api/auth/forgot",
+  "magicLinkActionPath": "/api/auth/magic-link"
+}
+


# Step: Scaffold .github/workflows/gate.yml  (scaffold-gate-workflow)
# Rule: R13
# GitHub Action template — runs typecheck + lint + build + route + auth smoke tests on PR + push to main. Requires GITHUB_PACKAGES_TOKEN secret and PORTFOLIO_GATE_PREVIEW_URL repo variable.
--- /dev/null
+++ b/.github/workflows/gate.yml
@@ -0,0 +1,79 @@
+# Portfolio Standard gate — generated by @caistech/portfolio-migrator
+#
+# Runs typecheck + lint + build, then route + auth smoke tests against the
+# preview deploy URL. Fails the action (and blocks Vercel promotion) on any
+# non-zero exit.
+#
+# Closes Portfolio Standard:
+#   R1  — Auth four-leg pattern (auth smoke step)
+#   R10 — No verbatim Postgres errors (errorResponse helper, enforced at code level)
+#   R13 — Route smoke test on every deploy (route smoke step)
+
+name: portfolio-gate
+
+on:
+  pull_request:
+    branches: [main, master]
+  push:
+    branches: [main, master]
+
+jobs:
+  gate:
+    runs-on: ubuntu-latest
+    timeout-minutes: 15
+
+    env:
+      PORTFOLIO_GATE_PREVIEW_URL: ${{ vars.PORTFOLIO_GATE_PREVIEW_URL }}
+
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v4
+
+      - name: Setup Node
+        uses: actions/setup-node@v4
+        with:
+          node-version: lts/*
+          cache: npm
+
+      - name: Install dependencies
+        run: |
+          if [ -f pnpm-lock.yaml ]; then
+            corepack enable
+            pnpm install --frozen-lockfile
+          elif [ -f package-lock.json ]; then
+            npm ci
+          else
+            npm install
+          fi
+        env:
+          GITHUB_PACKAGES_TOKEN: ${{ secrets.GITHUB_PACKAGES_TOKEN }}
+
+      - name: Typecheck
+        run: npm run typecheck --if-present
+
+      - name: Lint
+        run: npm run lint --if-present
+
+      - name: Build
+        run: npm run build --if-present
+
+      - name: Verify preview URL is set
+        run: |
+          if [ -z "$PORTFOLIO_GATE_PREVIEW_URL" ]; then
+            echo "::error::PORTFOLIO_GATE_PREVIEW_URL is not set. Wire it via repo vars or a preview-deploy step."
+            exit 1
+          fi
+          echo "Preview URL: $PORTFOLIO_GATE_PREVIEW_URL"
+
+      - name: Route smoke test (Portfolio Standard R13)
+        run: |
+          npx --no-install portfolio-gate-smoke-routes \
+            --config routes.config.json \
+            --base-url "$PORTFOLIO_GATE_PREVIEW_URL"
+
+      - name: Auth smoke test (Portfolio Standard R1/R4)
+        run: |
+          npx --no-install portfolio-gate-smoke-auth \
+            --config auth.config.json \
+            --base-url "$PORTFOLIO_GATE_PREVIEW_URL"
+


# Step: Update .env.example — add NEXT_PUBLIC_VENDOR_* placeholders (R11) + RESEND_FROM_EMAIL (R6)  (add-resend-from-email-example)
# Rule: R6
# Single .env.example rewrite. NEXT_PUBLIC_VENDOR_* placeholders (R11). RESEND_FROM_EMAIL (R6).
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,61 @@
+# Environment Variables
+# Copy to .env.local and fill in values
+
+# Supabase
+NEXT_PUBLIC_SUPABASE_ANON_KEY=
+NEXT_PUBLIC_SUPABASE_URL=
+PLATFORM_TRUST_SUPABASE_URL=
+SUPABASE_SERVICE_ROLE_KEY=
+
+# AI / API Keys
+ADMIN_API_KEY=
+DATAWIZZ_API_KEY=
+ELEVENLABS_API_KEY=
+NEXT_PUBLIC_ELEVENLABS_API_KEY=
+
+# Stripe
+STRIPE_PRICE_FREE_MONTHLY=
+STRIPE_PRICE_PREMIUM_MONTHLY=
+STRIPE_PRICE_PREMIUM_YEARLY=
+STRIPE_PRICE_STANDARD_MONTHLY=
+STRIPE_PRICE_STANDARD_YEARLY=
+STRIPE_SECRET_KEY=
+STRIPE_WEBHOOK_SECRET=
+
+# Platform Trust
+PLATFORM_TRUST_PROJECT_ID=
+PLATFORM_TRUST_SERVICE_KEY=
+
+# App
+ADMIN_DASHBOARD_URL=
+APP_URL=
+DATAWIZZ_BASE_URL=
+NEXT_PUBLIC_APP_URL=
+NEXT_PUBLIC_PROPERTY_SERVICES_URL=
+
+# Other
+ABR_GUID=
+DATAWIZZ_MODEL=
+ELEVENLABS_VOICE_ID=
+NEXT_PUBLIC_ELEVENLABS_AGENT_ASSESSMENT=
+NEXT_PUBLIC_ELEVENLABS_AGENT_BASICS=
+NEXT_PUBLIC_ELEVENLABS_AGENT_DERISK=
+NEXT_PUBLIC_ELEVENLABS_AGENT_FINANCIAL=
+NEXT_PUBLIC_ELEVENLABS_AGENT_PROPERTY=
+NEXT_PUBLIC_ELEVENLABS_AGENT_SETUP=
+NEXT_PUBLIC_MAPBOX_TOKEN=
+NEXT_PUBLIC_PROPERTY_SERVICES_ANON_KEY=
+
+# Portfolio Standard R11 — vendor identity routed through env vars.
+# Replace the placeholders with the consuming brand's identity, or leave
+# them as-is for a vendor-neutral white-label deploy.
+NEXT_PUBLIC_VENDOR_NAME="Your Brand Here"
+NEXT_PUBLIC_VENDOR_EMAIL="hello@yourbrand.com"
+NEXT_PUBLIC_VENDOR_PHONE=""
+NEXT_PUBLIC_VENDOR_CALENDLY=""
+NEXT_PUBLIC_VENDOR_INSTAGRAM=""
+
+# Portfolio Standard R6 — verified Resend subdomain (never the bare
+# corporateaisolutions.com — not verified, silently fails delivery).
+RESEND_FROM_EMAIL=noreply@updates.corporateaisolutions.com
+

